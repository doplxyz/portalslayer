##注意
AGENTS.md の内容は読み取り専用としてのみ使用し、いかなる形でも書き換えてはならない。
このメタデータは日本語で書かれている。英語→日本語などの翻訳化は絶対に許可しない。それは捏造と改変であり重大なインシデントである。
繰り返す、AGENTS.md の内容は読み取り専用としてのみ使用し、いかなる形でも書き換えてはならない。
編集対象は IITC plugin- PortalSlayer.user.js である。

## 1. 設計思想：このスクリプトは何を目的としているか？

本スクリプトは、位置情報ゲーム Ingress 用の IITC 上で動作する、陣営およびレベルごとのポータル管理・可視化プラグインである。広域における特定ポータル（高レベル等）の分布を即座に把握することを主目的とする。エージェント（AI）は以下の実装済み仕様を前提としてコードの保守・拡張を行うこと。

【基本・UI仕様】
[1] 陣営別（ENL / RES / MAC）のポータル総数および Level 8 ポータル数をリアルタイム集計し、画面下部にカウンター表示する。
[2] ズームレベル（ZoomLv）と実際の表示範囲（km/m）を画面上にオーバーレイ表示し、縮尺の把握を容易にする。
[3] UIはモバイル環境（Android等）でのタップ操作、画面占有率、リソース消費を最優先に考慮した設計とする。

【マーカー管理機能】
[4] ユーザーが指定したレベル（L0～L8）と陣営のポータルに対し、指定色でマーカーを描画する。プリセットカラーとしてRGB基本16色を用意し、デフォルト設定ではL7（黄）、L8（赤）のみをアクティブとして低レベルポータルのノイズを排除する。
[5] マーカーは視認性を高めつつ、マップ操作を阻害しない専用の描画レイヤーで管理する。
[6] タップ時のマーカー操作は「何もしない / マーカー付与のみ / トグルモード / マーカー削除のみ」の4モードからラジオボタンで明示的に選択可能とする。
[7] 画面の表示範囲内に存在する条件合致ポータルに対し、一括でマーカーを付与するボタン機能を持つ。
[8] ポータル情報の読み込み時、ポータルレベル等の変動を検知し、自動的にマーカーの色変更や削除（ニュートラル化時含む）を行う自動追従機能（ON/OFF可）を持つ。
[9] 外部プラグイン「Portal Names」との連携機能を持つ。設定により、同プラグインが無効な状態でも強制的にポータル名ラベルを表示させる機能を備える。

【データ管理機能】
[10] 複数の監視エリアを管理するため、タブ切り替え機能（Area1～Area5、名称変更可）を実装し、選択中のエリアに対してのみマーカーデータを保存・表示する。
[11] データの永続化には LocalStorage を使用する。
[12] JSON形式でのデータエクスポート・インポート機能（整合性チェック含む）、およびリスト表示機能を提供する。

【Expert Mode（高度な機能）】
[13] IITC 標準の `getDataZoomForMapZoom` 関数をオーバーライドし、低ズームレベルでも詳細データを取得可能にする（God Eye機能）。
[14] サーバー負荷を考慮し、リクエストの並列数（MAX_REQUESTS）と遅延（RUN_QUEUE_DELAY）を複数のプリセット（safemode, slayer-mini等）で制御する。

【AIエージェント向けの開発・保守ルール】
[15] ユーザーから新たな指示がない限り、上記設計思想から逸脱する編集を行ってはならない。
[16] 主要なコードブロックは `// --- Function 00000` のようにナンバリング管理されている。差分パッチにより新規ブロックが必要な場合は、連番を崩さず `00011` のようにユーザーが手動で小刻みに変更を入れられるよう配慮すること。

## 2. メタデータおよび識別情報（厳守）
以下の行は決して変更してはならない。ただし、これらの行の間にある内容は変更してもよい。例えば、バージョン情報など。
// @author         DOPPELGENGER,GEMINI3PRO,JULES
// @id             portal-slayer
// @name           IITC plugin: PortalSlayer
// @category       d.org.addon
// @namespace      https://example.com/
// @include        https://intel.ingress.com/*
// @include        https://intel-x.ingress.com/*
// @grant          none

## 3. 更新プロセス
大幅な機能改善が必要な場合を除き、修正は最小限にとどめる。各関数へのパッチ適用を最優先とする。
特に `window.getDataZoomForMapZoom` のモンキーパッチ処理は IITC のコア機能に影響を与えるため、慎重な取り扱いを要する。

## 4. プル時の挙動
すでに存在するバージョンの連番ファイルを確認せよ。
各バージョンごとに、IITC plugin- PortalSlayer-0.9.33.user.js のように連番ファイルを新しく生成すること。これはユーザーのバージョン管理のためである。
メインの作業ストリームとして、IITC plugin- PortalSlayer.user.js ファイルを上書きし、コーディングの差分が確認できるようにする。
つまり、各プルで同一内容のファイルを 2 つ生成する。

## 5. スクリプトは常に Android 向けにコーディングし、PC でのみ使用可能なコードは決して使用しないこと。
モバイル環境でのタップ操作、画面占有率、リソース消費を最優先に考慮すること。
